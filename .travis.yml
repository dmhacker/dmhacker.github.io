language: node_js
node_js:
  - '10'
install: 
  - yarn install 
  - yarn build
  - ls -la
os: linux
dist: xenial
deploy:
  strategy: git
  local_dir: public
  provider: pages
  token: $GITHUB_TOKEN
  cleanup: false
  on:
    branch: master
  target_branch: gh-pages
  keep_history: true
